import 'package:flutter/material.dart';
import '../pantalla/pantalla_carrito_compras.dart';

/// Ensambla la experiencia del carrito.
class ArbolCarritoCompras extends StatelessWidget {
  const ArbolCarritoCompras({super.key});
  @override
  Widget build(BuildContext context) => const PantallaCarritoCompras();
}
