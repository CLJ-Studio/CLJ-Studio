import 'repositorio_inicio_marketplace.dart';

/// Fachada preparada para añadir carga, errores y llamadas HTTP.
class ServicioInicioMarketplace {
  const ServicioInicioMarketplace(this.repositorio);
  final RepositorioInicioMarketplace repositorio;
}
